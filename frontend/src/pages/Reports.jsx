import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {getCases,getCaseAnalyses,reportPdf} from '../api';
export default function Reports(){const [rows,setRows]=useState([]);useEffect(()=>{(async()=>{const cs=await getCases().catch(()=>[]);const out=[];for(const c of cs.slice(0,20)){const a=await getCaseAnalyses(c.id).catch(()=>[]);const arr=Array.isArray(a)?a:(a?.items||[]);arr.forEach(x=>out.push({case:c,analysis:x}))}setRows(out)})()},[]);return <div className="panel"><div className="panel-head"><div><div className="eyebrow">EVIDENCE REPORTING</div><h3>Forensic Reports</h3><p>Generate the backend PDF evidence report for an analysis.</p></div></div><div className="report-grid">{rows.map(x=><div className="report-card" key={x.analysis.id}><div className="report-icon">PDF</div><div><b>{x.case.title||x.case.name||`Case ${x.case.id}`}</b><span>Analysis {x.analysis.id}</span><small>Forensic findings · IOCs · evidence metadata</small></div><a className="btn ghost" target="_blank" rel="noreferrer" href={reportPdf(x.analysis.id)}>Open PDF</a></div>)}{!rows.length&&<div className="empty">No analyses available yet.</div>}</div></div>}
