import React,{useEffect,useMemo,useState} from 'react';import {useNavigate} from 'react-router-dom';import {ShieldAlert,ChevronLeft,FileText,Lock,Network,Map,GitBranch,Paperclip,BrainCircuit,Download,Plus,Search,CheckCircle2} from 'lucide-react';import {advancedApi,casesApi,reportsApi} from '../api/modules';import {getSession,roleOf} from '../utils/auth';import {can} from '../utils/permissions';import {PageHeader,GlassCard,Section,RiskBadge,StatusBadge,LoadingState,ErrorState,DataTable,EmptyState,CopyButton,Modal} from '../components/ui';import ThreatMap from '../components/ThreatMap';
const tabs=[['overview','Overview','case:view'],['authentication','Authentication','authentication:view'],['relay','Relay Timeline','relay:view'],['infrastructure','Infrastructure','infrastructure:view'],['iocs','IOCs','ioc:view'],['findings','Findings','case:view'],['evidence','Evidence','evidence:view'],['graph','IOC Graph','graph:view'],['campaigns','Campaigns','campaign:view'],['features','Features','investigation:view'],['reports','Reports','report:view']];
function pick(d,...keys){for(const k of keys)if(d?.[k]!==undefined)return d[k];return undefined}
export default function Investigation({caseId}){const [data,setData]=useState(null),[err,setErr]=useState(null),[tab,setTab]=useState('overview');const [modal,setModal]=useState(false);const role=roleOf(getSession());useEffect(()=>{advancedApi.search(caseId).then(r=>{const arr=r.data?.items||r.data?.cases||r.data;const found=Array.isArray(arr)?arr.find(x=>String(x.case_id||x.id)===String(caseId)):null;if(found)setData(found);else casesApi.get(caseId).then(x=>setData(x.data))}).catch(()=>casesApi.get(caseId).then(r=>setData(r.data)).catch(setErr))},[caseId]);if(err)return <ErrorState error={err}/>;if(!data)return <LoadingState text="Loading forensic case workspace…"/>;const analysis=data.analysis||data.advanced_analysis||data;return <><button className="back-btn" onClick={()=>history.back()}><ChevronLeft size={16}/>Cases</button><PageHeader eyebrow={`CASE / ${caseId}`} title={data.subject||'Investigation workspace'} subtitle={data.sender||data.email||'Forensic investigation'} actions={<><RiskBadge level={data.risk_level||data.risk}/><StatusBadge status={data.status}/></>}/><div className="case-strip"><div><span>Case ID</span><b>{caseId}</b></div><div><span>Classification</span><b>{data.classification||'—'}</b></div><div><span>Risk score</span><b>{data.risk_score??'—'}</b></div><div><span>Assigned investigator</span><b>{data.assigned_to||'—'}</b></div></div><div className="tabs">{tabs.filter(t=>can(role,t[2])).map(t=><button className={tab===t[0]?'active':''} onClick={()=>setTab(t[0])} key={t[0]}>{t[1]}</button>)}</div>{tab==='overview'&&<Overview data={analysis}/>} {tab==='authentication'&&<Authentication data={analysis}/>} {tab==='relay'&&<Relay data={analysis}/>} {tab==='infrastructure'&&<Infrastructure data={analysis}/>} {tab==='iocs'&&<IOCs data={analysis}/>} {tab==='findings'&&<Findings data={analysis} canEdit={can(role,'finding:create')} onAdd={()=>setModal(true)}/>} {tab==='evidence'&&<Evidence data={analysis}/>} {tab==='graph'&&<Graph data={analysis}/>} {tab==='campaigns'&&<Campaigns data={analysis}/>} {tab==='features'&&<Features data={analysis}/>} {tab==='reports'&&<CaseReports caseId={caseId}/>}<Modal open={modal} onClose={()=>setModal(false)} title="Add forensic finding" actions={<button className="primary" onClick={()=>setModal(false)}>Save finding</button>}><label>Finding<input placeholder="Finding title"/></label><label>Severity<select><option>HIGH</option><option>MEDIUM</option><option>LOW</option><option>CRITICAL</option></select></label><label>Notes<textarea placeholder="Analyst evidence and rationale…"/></label></Modal></>}
const rows=(d,k)=>Array.isArray(d?.[k])?d[k]:[];
function Overview({data}){const findings=rows(data,'findings');const rb=data.risk_breakdown||{};return <><div className="hero-grid"><GlassCard className="risk-score"><span>Overall Risk Score</span><strong>{data.risk_score??data.score??'—'}</strong><RiskBadge level={data.risk_level||data.risk}/><small>System-generated risk assessment</small></GlassCard><GlassCard><div className="card-title">Classification</div><h3>{data.classification||'—'}</h3><p>Confidence: {data.confidence??'—'}</p></GlassCard><GlassCard><div className="card-title">Why this email was flagged</div>{findings.length?<ul className="finding-list">{findings.slice(0,6).map((f,i)=><li key={i}><ShieldAlert size={16}/><span>{f.title||f.finding||f.description||'Finding returned by backend'}</span></li>)}</ul>:<EmptyState title="No findings returned" text="The case analysis has not supplied forensic findings."/>}</GlassCard></div><Section title="Risk Breakdown" subtitle="System scoring components returned by the analysis service"><div className="score-grid">{[['ML Detection',rb.ml_score??rb.ml??'—'],['Forensic Evidence',rb.forensic_score??rb.forensic??'—'],['Advanced Intelligence',rb.advanced_score??rb.advanced??'—']].map(([l,v])=><GlassCard key={l}><span>{l}</span><strong>{v}{v!=='—'?'%':''}</strong><div className="progress"><i style={{width:v==='—'?'0':`${Math.min(100,Number(v))}%`}}/></div></GlassCard>)}</div></Section></>}
function Authentication({data}){const a=data.authentication||data.auth||{};return <Section title="Email Authentication" subtitle="Forensic comparison of identity and authentication signals"><div className="auth-grid">{['spf','dkim','dmarc'].map(k=><GlassCard key={k}><span>{k.toUpperCase()}</span><div className="auth-status">{a[k]?.status||a[k]||'UNKNOWN'}</div><small>{a[k]?.reason||'No explanation returned by backend.'}</small></GlassCard>)}</div><GlassCard className="forensic-fields"><b>Header identity</b>{['from','return_path','reply_to','authentication_results'].map(k=><div key={k}><span>{k.replaceAll('_',' ')}</span><code>{a[k]||'—'}</code><CopyButton value={a[k]}/></div>)}</GlassCard></Section>}
function Relay({data}){const hops=rows(data,'relay_timeline').length?rows(data,'relay_timeline'):rows(data,'received_headers');return <Section title="Relay Timeline" subtitle="Received-header infrastructure path"><div className="timeline">{hops.length?hops.map((h,i)=><div className="timeline-item" key={i}><div className="timeline-dot"/><GlassCard><div className="timeline-head"><b>{h.hostname||h.host||'Mail infrastructure'}</b><span>{h.timestamp||'—'}</span></div><div className="mini-grid"><span>IP <b>{h.ip||'—'}</b></span><span>Provider <b>{h.provider||'—'}</b></span><span>Country <b>{h.country||'—'}</b></span><span>ASN <b>{h.asn||'—'}</b></span></div></GlassCard></div>):<EmptyState title="No relay hops returned" text="Received headers or relay telemetry are not available for this case."/>}</div><div className="notice">Infrastructure geolocation is contextual intelligence and does not prove the physical identity/location of the sender.</div></Section>)}
function Infrastructure({data}){return <Section title="Infrastructure Map" subtitle="GeoIP context for infrastructure indicators"><ThreatMap points={rows(data,'infrastructure').concat(rows(data,'relay_timeline'))}/></Section>}
function IOCs({data}){const list=rows(data,'iocs').length?rows(data,'iocs'):rows(data,'indicators');return <Section title="IOC Intelligence" subtitle="Indicators extracted or associated with this investigation"><DataTable rows={list} columns={[{key:'type',label:'Type'},{key:'value',label:'Value',render:r=><span className="mono">{r.value||r.indicator||'—'}</span>},{key:'source',label:'Source'},{key:'risk',label:'Risk',render:r=><RiskBadge level={r.risk}/>} ,{key:'confidence',label:'Confidence'},{key:'first_seen',label:'First Seen'},{key:'related_cases',label:'Related Cases'}]}/></Section>}
function Findings({data,canEdit,onAdd}){const list=rows(data,'findings');return <Section title="Forensic Findings" subtitle="Evidence-backed observations from analysis" actions={canEdit&&<button className="secondary" onClick={onAdd}><Plus size={16}/>Add finding</button>}><DataTable rows={list} columns={[{key:'title',label:'Finding',render:r=>r.title||r.finding},{key:'severity',label:'Severity',render:r=><RiskBadge level={r.severity}/>} ,{key:'category',label:'Category'},{key:'evidence',label:'Evidence'},{key:'confidence',label:'Confidence'},{key:'status',label:'Status',render:r=><StatusBadge status={r.status}/>} ]}/></Section>}
function Evidence({data}){const list=rows(data,'evidence');return <Section title="Evidence & Chain of Custody" subtitle="Integrity state returned by the evidence service"><DataTable rows={list} columns={[{key:'evidence_id',label:'Evidence ID'},{key:'file_name',label:'File name'},{key:'sha256',label:'SHA-256',render:r=><span className="hash">{r.sha256||'—'}</span>},{key:'collected_at',label:'Collection time'},{key:'source',label:'Source'},{key:'integrity_status',label:'Integrity',render:r=><span className="integrity"><CheckCircle2 size={14}/>{r.integrity_status||'—'}</span>}]}/>{list.length&&<div className="chain">{['Collected','Hashed','Analyzed','Stored','Reported'].map((x,i)=><div key={x}><span>{i+1}</span><b>{x}</b></div>)}</div>}</Section>}
function Graph({data}){const g=data.graph_json||data.graph||null;return <Section title="IOC Graph" subtitle="Relationship graph returned by backend"><GlassCard className="graph"><GitBranch size={25}/><h3>{g?'Investigation graph loaded':'No graph data returned'}</h3><p>{g?'Render the returned nodes and edges here.':'The backend has not returned graph_json for this case.'}</p></GlassCard></Section>}
function Campaigns({data}){const c=data.campaign||data.campaigns;return <Section title="Campaign Correlation" subtitle="Heuristic relationships must not be interpreted as confirmed attribution"><GlassCard className="campaign"><Layers3Icon/><div><h3>{c?.name||c?.id||'No campaign cluster returned'}</h3><p>{c?.summary||'Shared infrastructure and indicators will appear when returned by the backend.'}</p></div></GlassCard></Section>}
function Layers3Icon(){return <Layers3/>}
const featureList=['Multilingual Detection','BEC / Impersonation','Lookalike Domain Detection','URL Obfuscation','Cross-Case Campaign Correlation','Infrastructure Fingerprinting','Explainable Risk Breakdown','Investigation Graph','Evidence Hash Chain','Privacy Mode','Attachment Forensics','QR / Quishing Detection','Gmail Alert Integration','Natural Language Investigation Search','Analyst Copilot','Advanced Reports','Advanced Analytics'];
function Features({data}){const f=data.features||{};return <Section title="Advanced Intelligence" subtitle="Implementation status is derived from backend capability signals"><div className="feature-grid">{featureList.map(x=>{const key=x.toLowerCase().replace(/[^a-z0-9]+/g,'_');const v=f[key]??f[x];return <GlassCard key={x}><div className="feature-top"><span>{x}</span><StatusBadge status={v?.status||'HOOK_AVAILABLE'}/></div><p>{v?.description||'Backend capability signal not provided. UI does not infer completion.'}</p>{v?.signal&&<small>Signal: {v.signal}</small>}</GlassCard>})}</div></Section>}
function CaseReports({caseId}){const [busy,setBusy]=useState(false);const generate=async()=>{setBusy(true);try{await reportsApi.generate({case_id:caseId,type:'forensic_pdf'})}finally{setBusy(false)}};return <Section title="Reports" subtitle="Generate artifacts from backend-supported report types"><div className="report-actions"><button className="primary" onClick={generate} disabled={busy}><FileText size={17}/>{busy?'Generating…':'Generate Forensic PDF'}</button><button className="secondary"><Download size={17}/>JSON Evidence Report</button></div></Section>}
