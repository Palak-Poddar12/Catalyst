import React from 'react';import {ShieldCheck,KeyRound,LogOut} from 'lucide-react';import {getSession,roleOf} from '../utils/auth';import {ROLE_PERMISSIONS} from '../utils/permissions';import {PageHeader,GlassCard,Section} from '../components/ui';
export default function Profile(){const s=getSession(),role=roleOf(s);return <><PageHeader eyebrow="ACCOUNT" title="Profile" subtitle="Identity, permissions and session posture."/><div className="profile-grid"><GlassCard className="profile-card"><div className="avatar xl">{(s?.user?.name||s?.user?.email||'S').slice(0,1).toUpperCase()}</div><h2>{s?.user?.name||'SatGuard User'}</h2><p>{s?.user?.email||'—'}</p><span className="role-pill">{role}</span></GlassCard><Section title="Permissions" subtitle="Frontend permission gates mirror backend authorization intent"><div className="permission-chips">{ROLE_PERMISSIONS[role].map(p=><span key={p}>{p}</span>)}</div></Section></div><Section title="Security" subtitle="Prototype controls are displayed only when backend support exists"><div className="security-actions"><button className="secondary"><KeyRound size={16}/>Change password</button><button className="secondary"><LogOut size={16}/>Logout all sessions</button></div></Section></>}
