import React,{useState} from 'react';import {useSearchParams} from 'react-router-dom';import {Search,Globe2,ShieldCheck,Database,ExternalLink} from 'lucide-react';import {advancedApi,intelApi} from '../api/modules';import {PageHeader,GlassCard,Section,RiskBadge,LoadingState,ErrorState,EmptyState,CopyButton,DataTable} from '../components/ui';
export default function Intelligence(){const [sp]=useSearchParams();const [q,setQ]=useState(sp.get('q')||'');const [type,setType]=useState('search');const [data,setData]=useState(null),[busy,setBusy]=useState(false),[err,setErr]=useState(null);async function lookup(){if(!q.trim())return;setBusy(true);setErr(null);try{let r;if(type==='ip')r=await intelApi.ip(q.trim());else if(type==='domain')r=await intelApi.domain(q.trim());else if(type==='url')r=await intelApi.url(q.trim());else r=await advancedApi.search(q.trim());setData(r.data)}catch(e){setErr(e)}finally{setBusy(false)}}return <><PageHeader eyebrow="THREAT INTELLIGENCE" title="Intelligence Lookup" subtitle="Search infrastructure and investigation data through the existing backend."/><div className="lookup"><select value={type} onChange={e=>setType(e.target.value)}><option value="search">Investigation Search</option><option value="ip">IP</option><option value="domain">Domain</option><option value="url">URL</option></select><div className="lookup-input"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&lookup()} placeholder="IP, domain, URL or natural-language investigation query"/></div><button className="primary" onClick={lookup}>Investigate</button></div>{busy?<LoadingState text="Querying threat intelligence…"/>:err?<ErrorState error={err}/>:data?<Results data={data}/>:<EmptyState title="Start an intelligence lookup" text="No external intelligence is fabricated. Results are shown only when the backend returns them."/>}</>}
function Results({data}){const obj=data?.result||data;const rows=Array.isArray(obj?.items)?obj.items:Array.isArray(obj)?obj:[];return <div className="intel-grid"><Section title="Reputation & Context" subtitle="Backend-returned intelligence"><div className="intel-cards">{['reputation','country','city','asn','provider','confidence','risk'].map(k=><GlassCard key={k}><span>{k}</span><strong>{obj?.[k]??'—'}</strong></GlassCard>)}</div></Section><Section title="Related Indicators" subtitle="No assumptions beyond returned data"><DataTable rows={rows} columns={[{key:'type',label:'Type'},{key:'value',label:'Value'},{key:'source',label:'Source'},{key:'risk',label:'Risk',render:r=><RiskBadge level={r.risk}/>} ,{key:'confidence',label:'Confidence'}]}/></Section></div>}
