import React,{useState} from 'react';import {useNavigate} from 'react-router-dom';import {Shield,LockKeyhole,Eye,EyeOff,Wifi,ArrowRight,CheckCircle2} from 'lucide-react';import {authApi} from '../api/modules';import {saveSession} from '../utils/auth';
const demos=[['ADMIN','admin@satguard.local','admin123'],['ANALYST','analyst@satguard.local','analyst123'],['INVESTIGATOR','investigator@satguard.local','invest123'],['VIEWER','viewer@satguard.local','viewer123']];
export default function Login(){const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [remember,setRemember]=useState(true);const [busy,setBusy]=useState(false);const [err,setErr]=useState('');async function submit(e){e.preventDefault();setBusy(true);setErr('');try{const r=await authApi.login({email,password});const data=r.data;const token=data.access_token||data.token||data.accessToken;if(token)(remember?localStorage:sessionStorage).setItem('satguard_token',token);saveSession(data,remember);nav('/dashboard')}catch(e){setErr(e.response?.data?.detail||'Sign-in failed. Check your credentials and API configuration.')}finally{setBusy(false)}}return <div className="login-page app-bg"><div className="login-glow"/><div className="login-card"><div className="login-brand"><div className="brand-mark big"><Shield size={27}/></div><div><b>SATGUARD</b><span>Email Threat Intelligence & Forensics</span></div></div><div className="login-title"><span className="eyebrow">SECURE SOC ACCESS</span><h1>Welcome to SatGuard</h1><p>Sign in to investigate and secure your email infrastructure.</p></div><form onSubmit={submit} className="login-form"><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="username" placeholder="analyst@organization.local" required/></label><label>Password><div className="password"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} autoComplete="current-password" placeholder="••••••••" required/><button type="button" className="icon-btn" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{err&&<div className="login-error">{err}</div>}<div className="login-options"><label className="check"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Remember me</label><button type="button" className="text-btn">Forgot password?</button></div><button className="primary full" disabled={busy}>{busy?'Authenticating…':'Sign In'}<ArrowRight size={17}/></button></form><div className="demo-box"><div className="demo-head"><span>Demo Accounts</span><small>Prototype access</small></div><div className="demo-grid">{demos.map(([role,e,p])=><button key={role} onClick={()=>{setEmail(e);setPassword(p)}}><span>{role}</span><small>{e}</small></button>)}</div></div><div className="secure"><Wifi size={15}/><span>Secure connection</span><CheckCircle2 size={14}/></div></div></div>}
