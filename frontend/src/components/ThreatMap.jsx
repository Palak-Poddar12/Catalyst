import React from 'react';import {MapContainer,TileLayer,Marker,Popup,CircleMarker} from 'react-leaflet';import L from 'leaflet';import {RiskBadge} from './ui';delete L.Icon.Default.prototype._getIconUrl;L.Icon.Default.mergeOptions({iconRetinaUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png',iconUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png',shadowUrl:'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png'});
export default function ThreatMap({points=[]}){const valid=points.filter(p=>Number.isFinite(Number(p.lat))&&Number.isFinite(Number(p.lng)));return <div className="threat-map"><MapContainer center={valid[0]?[valid[0].lat,valid[0].lng]:[22.9734,78.6569]} zoom={valid.length?4:4} scrollWheelZoom><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{valid.map((p,i)=><Marker key={p.id||i} position={[p.lat,p.lng]}><Popup><div className="map-popup"><b>{p.ip||p.value||'Infrastructure'}</b><span>{p.city||'Unknown city'}, {p.country||'Unknown country'}</span><span>ASN: {p.asn||'—'}</span><span>Provider: {p.provider||'—'}</span><RiskBadge level={p.risk||'UNKNOWN'}/></div></Popup></Marker>)}{!valid.length&&<CircleMarker center={[22.9734,78.6569]} radius={8}><Popup>Demo fallback center only — no backend GeoIP coordinates were returned.</Popup></CircleMarker>}</MapContainer><div className="map-note">Infrastructure geolocation is contextual intelligence and does not prove the physical identity or location of the sender.</div></div>}
