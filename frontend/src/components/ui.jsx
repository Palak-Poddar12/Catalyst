import React from 'react';import {Loader2,AlertTriangle,CheckCircle2,Copy,ExternalLink,Search,X} from 'lucide-react';
export const GlassCard=({children,className=''})=><section className={`glass-card ${className}`}>{children}</section>;
export const PageHeader=({eyebrow,title,subtitle,actions})=><div className="page-header"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{actions&&<div className="header-actions">{actions}</div>}</div>;
export const RiskBadge=({level})=>{const v=String(level||'UNKNOWN').toUpperCase();return <span className={`badge risk-${v.toLowerCase()}`}>{v}</span>};
export const StatusBadge=({status})=><span className="badge status"><span className="status-dot"/>{String(status||'UNKNOWN').replaceAll('_',' ')}</span>;
export const LoadingState=({text='Loading intelligence…'})=><div className="state"><Loader2 className="spin" size={22}/><span>{text}</span></div>;
export const ErrorState=({error})=><div className="state error"><AlertTriangle size={22}/><span>{error?.response?.data?.detail||error?.message||'Unable to load this resource.'}</span></div>;
export const EmptyState=({title='No data available',text='The backend returned no records for this view.'})=><div className="state"><div><strong>{title}</strong><span>{text}</span></div></div>;
export const StatCard=({label,value,meta,icon:Icon,tone=''})=><GlassCard className="stat-card"><div className={`stat-icon ${tone}`}>{Icon&&<Icon size={19}/>}</div><div className="stat-copy"><span>{label}</span><strong>{value??'—'}</strong>{meta&&<small>{meta}</small>}</div></GlassCard>;
export const Section=({title,subtitle,actions,children})=><div className="section"><div className="section-head"><div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div>{actions}</div>{children}</div>;
export const DataTable=({columns,rows,empty='No records found.'})=><div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={r.id||i}>{columns.map(c=><td key={c.key}>{c.render?c.render(r):r[c.key]??'—'}</td>)}</tr>):<tr><td colSpan={columns.length}><EmptyState text={empty}/></td></tr>}</tbody></table></div>;
export const Modal=({open,onClose,title,children,actions})=>open?<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><div className="modal-head"><h3>{title}</h3><button className="icon-btn" onClick={onClose}><X size={18}/></button></div><div className="modal-body">{children}</div>{actions&&<div className="modal-actions">{actions}</div>}</div></div>:null;
export const CopyButton=({value})=>{const [done,setDone]=React.useState(false);return <button className="icon-btn" title="Copy" onClick={()=>{navigator.clipboard?.writeText(value||'');setDone(true);setTimeout(()=>setDone(false),1200)}}>{done?<CheckCircle2 size={15}/>:<Copy size={15}/>}</button>};
export const SearchInput=({value,onChange,placeholder='Search…',onSubmit})=><form className="search-input" onSubmit={e=>{e.preventDefault();onSubmit?.()}}><Search size={17}/><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder}/></form>;
